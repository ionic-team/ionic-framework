{
  "$schema": "http://json-schema.org/schema",
  "id": "ionicNgAdd",
  "title": "Ionic Add options",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the project.",
      "$default": {
        "$source": "projectName"
      }
    },
    "root": {
      "type": "string",
      "description": "The root of the project when it's a multiple application angular project"
    }
  },
  "required": []
}
