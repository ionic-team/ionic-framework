import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Enums/MedColors" />

# Enums

## MedColors

Enum representando as cores do design system.

```ts
import { MedColors } from '@ionic/core';
```

```ts
export enum MedColors {
  BRAND = 'brand',
  AULAS = 'aulas',
  MATERIAL = 'material',
  QUESTOES = 'questoes',
  REVALIDA = 'revalida',
  PROVAS = 'provas',
  WARNING = 'warning',
  CAUTION = 'error',
  SUCCESS = 'success',
}
```
