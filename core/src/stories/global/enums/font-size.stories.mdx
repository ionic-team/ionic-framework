import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Enums/MedFontSize" />

# Enums

## MedFontSize

Enum representando as cores do design system.

```ts
import { MedFontSize } from "@ionic/core";
```

```ts
export enum MedFontSize {
  XXXS = "12px",
  XXS = "14px",
  XS = "16px",
  SM = "20px",
  MD = "24px",
}
```
