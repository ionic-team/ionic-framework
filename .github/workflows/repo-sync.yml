on:
  schedule:
  - cron:  "*/15 * * * *"
  workflow_dispatch:

jobs:
  repo-sync:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
      with:
        persist-credentials: false
    - name: repo-sync
      uses: repo-sync/github-sync@v2.3.0
      with:
        source_repo: "ionic-team/ionic-framework"
        source_branch: "master"
        destination_branch: "sync"
        github_token: "7885d9f6d5bdf5d772bc908846d6f0c9a1abb4b0"
